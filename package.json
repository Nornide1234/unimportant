{
  "name": "keyserver",
  "version": "1.0.0",
  "private": true,
  "description": "Self-hosted license-key validation backend, protocol-compatible with what KeyAuthClient.h already sends (type=init/license/logout form POSTs).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "keys": "node addkey.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.12.0"
  }
}
